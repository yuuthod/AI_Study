# 데이터 모델: <도메인>

> 기획 수준의 엔티티·관계·규칙. **구현 진실원천은 `prisma/schema.prisma`.** 여기엔 *왜/규칙*만, 컬럼 타입 세부는 Prisma가 단독.

## 엔티티
| 엔티티 | 설명 | 핵심 속성(기획) |
|---|---|---|
| | | |

## 관계
- <A 1:N B 등, 관계의 의미>

## 비즈니스 규칙 / 제약
- <예: 이메일 유니크, 삭제 시 연쇄 동작, 소유권 규칙(인가)>

## Prisma 매핑 메모
- 위 엔티티 → `prisma/schema.prisma`의 어떤 model. 변경 시 `pnpm prisma migrate dev`.
