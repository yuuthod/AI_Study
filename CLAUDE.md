# 공유 스터디 저장소

## 개요

두 사람이 함께 주제를 정해 공부하고, 각자의 정리를 비교·토론하며 기록을 쌓아가는 스터디 저장소입니다.

---

## 참여자

| 닉네임   | git email                                     | 폴더명    |
| ----- | --------------------------------------------- | ------ |
| river | 44132562+yuuthod@users.noreply.github.com     | river/ |
| kon   | 109451148+konveloper@users.noreply.github.com | kon/   |

> **설정 확인**: `git config user.email` 출력값이 위 email과 일치해야 합니다.  
> 일치하지 않으면 스킬이 현재 사용자를 인식하지 못합니다.

---

## 저장소 구조

```
[큰 주제]/
  [작은 주제]/       ← 스킬은 이 폴더 안에서 실행
    river/           ← river의 정리 파일 (파일명·개수 자유)
    kon/             ← kon의 정리 파일 (파일명·개수 자유)
    _공통/           ← 스킬이 자동 생성 (직접 편집 불필요)
      비교분석.md
      다음주제.md
스터디로그.md        ← 전체 흐름 기록 (스킬C가 자동 관리)
```

---

## 스터디 흐름

```
1. [큰 주제]/[작은 주제]/{river,kon,_공통} 폴더 생성
2. 각자 정리 후 자신의 폴더에 파일 작성 → git commit → push
3. 모임에서 [작은 주제] 폴더 안에서 스킬 순서대로 실행:
   /study-compare → /study-next → /study-log
```

---

## 스킬 라우팅

| 상황 | 스킬 | 실행 위치 |
|------|------|---------|
| 두 사람 파일 비교 + 토론 | `/study-compare` | [작은 주제] 폴더 |
| 다음 주제 결정 | `/study-next` | [작은 주제] 폴더 |
| 스터디 로그 업데이트 | `/study-log` | 어디서든 가능 |

---

## 자동화 규칙

- 스킬 완료 시 자동으로 `git add / commit / push` 실행
- 스킬 실행 전 커밋되지 않은 개인 파일이 있으면 먼저 커밋 안내
- 현재 사용자는 `git config user.email`로 자동 감지 후 위 참여자 표와 대조
